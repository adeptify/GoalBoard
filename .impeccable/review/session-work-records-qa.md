disposition: ship

## scope

本结论只覆盖 Session 与工作目录合并后的工作台、高保真时间流、新建/关联 Session 弹窗，以及对应的桌面与移动端交互。验收基准为用户确认的两张高保真稿，而不是早期实现截图。

## fidelity

| 要素 | 结论 | 证据 |
|---|---|---|
| Session 单一入口 | match | 一级“工作目录”已移除；工作目录只作为 Session 的运行位置存在。 |
| 桌面三栏几何 | match | 1552×1014 视口实测：左栏 400px；正文 x=418、宽 1104px；时间流 783px；右栏 303px；正文起点 y=199。 |
| 左侧记录密度 | match | 第一条记录 y=239、高 92px；首屏可稳定阅读约 5 条记录，状态、Runtime、Goal 与时间层级清楚。 |
| 执行时间流 | match | 日期锚点、时间、连续轨道和语义节点组成纵向扫描路径；用户消息突出，Runtime 正文展开，工具/文件/测试压缩为横向证据行。 |
| 技术事件披露 | match | “查看详情 / 查看变更 / 展开输出”使用统一折叠交互与 chevron，不再表现为等权大卡片。 |
| 右侧关系上下文 | match | 当前关系与 Goal 历史保持固定辅助宽度和纵向高度，不挤占主要阅读流。 |
| 新建 Session 弹窗 | match | 实测 x=574、宽 540px、高 649px；标题切换在头部，已删除厚重的“启动方式”卡片；工作目录选项宽 304px且不遮挡底部操作区。 |
| 响应式 | match | 488px 宽下目录、正文和弹窗均无横向溢出；弹窗铺满视口并取消桌面偏移。 |
| 深色模式 | match | 页面、时间流、用户消息与技术证据行均使用中性近黑层级，正文对比度清楚，无横向溢出。 |

## browser_e2e

- 搜索唯一标题后仅 1 条记录可见，清空后恢复。
- 切到工具事件后 151 条可见，错误分组 0 条。
- 技术详情可展开并显示原始输出。
- 新建与“关联已有 Session”模式可互相切换，原生 Session 字段按模式出现。
- 工作目录下拉与底部操作区无重叠。
- 桌面浅色、桌面深色、移动目录、移动正文和移动弹窗均检查了横向溢出。

## automated_verification

- `npm run typecheck`：通过。
- `npm run build`：通过。
- Session 定向测试：5 / 5 通过。
- 受影响的项目范围回归：1 / 1 通过。
- Session + Web + Desktop 串行回归：93 / 96 通过；3 个失败来自并行中的 Risk、Onboarding 创建和旧数据迁移行为，均不经过 Session 工作台调用链。
- `git diff --check`：通过。

## material_fixes

- 把 Session 顶部收成与设计稿一致的单行标题栏。
- 把主正文从居中窄列恢复到设计稿的 1104px 工作面，并重建 783/303 两栏比例。
- 把工具、文件和测试事件从厚卡片改成带细分隔线的证据台账行。
- 删除弹窗中额外的启动方式卡片，将模式切换收回标题区；重新校准弹窗位置、下拉宽度和底部空间。

## keep

保持“对话直接阅读、技术证据随时可追溯但不抢正文”的权重。不要再把技术事件恢复成等权卡片，也不要把工作目录重新提升成一级模块。
